version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm install --prefix material-ui-dropzone
    build:
      commands:
        - cd material-ui-dropzone && npm run build
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*