version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - npm install --prefix material-ui-dropzone
    build:
      commands:
        - npm run build --prefix material-ui-dropzone
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*